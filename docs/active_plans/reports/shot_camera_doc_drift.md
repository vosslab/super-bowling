# Shot camera documentation drift

M0 record for the user-owned shot-camera planning note. This is a historical,
bounded source audit, not a general documentation review. Documentation is a
hypothesis about the code; M16 completed the listed corrections. The current
working-tree plan is untracked, so its line references were plan evidence, not
committed repository documentation.

## Verdicts

| Documentation claim                                                                                                                                                                          | Verdict                     | Current evidence                                                                                                                                                                                                                                         | M16 action                                                                                                                            |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `GEOMETRY_MODEL.md:85-88` says one immutable complete-rack solve applies throughout a shot and the renderer does not zoom, follow, or reframe.                                               | STALE                       | `src/render/camera.ts:238-278` advances focus and ratchets zoom; `src/render/camera.ts:76-79` holds the first ball-pin zone; `src/render/result_camera.ts:78-113` fits the authoritative settled snapshot.                                               | Describe fixed establishing and settled framing, zone-led travel, and the held collision view. Preserve the physical-scale statement. |
| `CODE_ARCHITECTURE.md:32-33` says committed SVG artwork is rasterized once into reusable Canvas assets.                                                                                      | ACCURATE                    | `src/render/game_assets.ts:37-49` rasterizes into a Canvas; `src/render/game_assets.ts:55-76` performs it after asset load; `src/render/game_renderer.ts:491-542` draws those reusable sources.                                                          | Keep the claim; M12 characterizes its dense-mode cost and minification limits.                                                        |
| `CODE_ARCHITECTURE.md:161-163` presents the simulation benchmark with other verification tools, without identifying its lack of production Canvas raster timing.                             | PARTLY STALE                | `package.json:12` maps `npm run benchmark` to `devel/run_simulation_benchmark.mjs`; `src/render/game_assets.ts:37-76` and `src/render/game_renderer.ts:491-542` are instead the production asset and Canvas draw paths.                                  | State that this is a simulation benchmark. M11 adds a production-render relative frame-cost probe.                                    |
| `DEVELOPMENT.md:51` directs physics scale or settlement performance to `npm run benchmark`.                                                                                                  | PARTLY STALE                | The command is the simulation-only script at `package.json:12`; it cannot measure the production Canvas asset and draw paths at `src/render/game_assets.ts:37-76` and `src/render/game_renderer.ts:491-542`.                                             | Retain it for simulation performance, and route raster/frame-cost work to the M11 render harness.                                     |
| `DEVELOPMENT.md:53-57` keeps timings and visual thresholds as `devel/` or artifact evidence rather than permanent assertions.                                                                | ACCURATE                    | The policy is now `docs/DEVELOPMENT.md:54-58`; deterministic camera coverage is `tests/test_camera.mjs`, while maintained probes belong under `devel/`.                                                                                                  | Keep the test-tier policy unchanged.                                                                                                  |
| `LANE_MASTER_VIDEO_FINDINGS.md:109` adopts a shot-driven shared projection.                                                                                                                  | ACCURATE                    | `src/render/game_renderer.ts:360-408` makes one projection for lane, pins, ball, and aim commands; `src/render/game_renderer.ts:630-655` makes a second only for opted-in diagnostics; rolling focus and zoom advance at `src/render/camera.ts:238-278`. | Extend the wording for collision-zone travel after its evidence lands.                                                                |
| `LANE_MASTER_VIDEO_FINDINGS.md:110` says the moving ball keeps volume through shade, gloss, reflection, holes, and shadow.                                                                   | ACCURATE                    | Ball presentation is produced by `src/render/ball.ts:58-205`; its command is drawn through `src/render/game_renderer.ts:491-542`.                                                                                                                        | Keep; M9 supplies the missing archetype evidence.                                                                                     |
| `LANE_MASTER_VIDEO_FINDINGS.md:111` says snapshot velocity drives pin lift, shadow, and one afterimage.                                                                                      | PARTLY STALE                | The one trailing exposure is bounded at `src/render/pins.ts:174-180`, while `src/render/game_renderer.ts:491-542` draws shadows only when its `draw_shadows` input is true; the live renderer passes true only through 105 pins at `:628-629`.           | Scope shadows to smaller modes and retain velocity-derived lift and afterimage where rendered.                                        |
| `LANE_MASTER_VIDEO_FINDINGS.md:112` keeps Rapier snapshots authoritative for every pin pose.                                                                                                 | ACCURATE                    | `src/simulation/worker.ts:60-72` publishes the world snapshot; `src/render/fallen_pin_presentation.ts:37-59` derives a visual pose without changing the physical state.                                                                                  | Keep unchanged.                                                                                                                       |
| `LANE_MASTER_VIDEO_FINDINGS.md:113-115` adopts distinct result bursts, keeps controls available, and rejects cabinet rewards.                                                                | ACCURATE                    | Result selection follows the authoritative `settled` event at `src/app/game.tsx:417-443`; the canvas and live control deck remain mounted at `src/app/game.tsx:897-960`.                                                                                 | Keep unchanged.                                                                                                                       |
| `LANE_MASTER_VIDEO_FINDINGS.md:146-151` requires smooth resettable camera progress, a shared projection, motion-only pin emphasis, distinct results, and one accessible status announcement. | ACCURATE                    | Monotonic rolling state is `src/render/camera.ts:238-278`; reduced-motion projection is isolated at `src/render/camera.ts:325-347`; settled framing starts from the authoritative event at `src/app/game.tsx:417-443`.                                   | Keep, then add M6-M10 measured camera criteria.                                                                                       |
| `LANE_MASTER_VIDEO_FINDINGS.md:151` calls ten-pin collisions dramatic without obscuring the final standing-pin state.                                                                        | ACCURATE, EVIDENCE DEFERRED | The settled camera derives its frame from the authoritative terminal snapshot at `src/render/result_camera.ts:78-113`; no current executable readability measurement establishes the live ten-pin travel claim.                                          | M9/M10 add ball residency, collision coverage, and result-handoff evidence.                                                           |
| `LANE_MASTER_VIDEO_FINDINGS.md:152` requires a complete readable wide deck rather than the ten-pin zoom.                                                                                     | STALE                       | `src/render/collision_zone.ts:185-218` builds the local collision subject; `src/render/camera.ts:238-278` composes it during travel; `src/render/result_camera.ts:78-113` fits the authoritative settled result.                                         | Require local collision-zone framing during travel and impact; require full-rack legibility only for establishing and settled frames. |
| `LANE_MASTER_VIDEO_FINDINGS.md:153` says shipped assets and layouts remain original to Super Bowling.                                                                                        | PARTLY STALE                | The fallen-pin asset declares an adapted public-domain OpenClipart silhouette at `src/assets/pin_fallen.svg:2`; project-specific rendering, layout, and behavior remain local.                                                                           | Say layouts and authored presentation are original while crediting the adapted public-domain silhouette.                              |

## M0 disposition

M0 is complete. No stale row invalidates M1's projection extraction premise. The
stale framing, dense-shadow, raster-benchmark, wide-rack, and asset-provenance
claims were bounded M16 corrections. Subsequent M9 evidence measures readable
ball travel and local-zone framing. M10 blocks only an impulse-only expansion,
because that rule would admit remote dense-rack contacts.

See [../../archive/shot_camera_collision_travel.md](../../archive/shot_camera_collision_travel.md)
for the final milestone status.
